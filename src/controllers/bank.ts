import { Response, Request, NextFunction, RequestHandler } from "express";

//export const createBankservice: RequestHandler = (req, res, next) => {};

export const currentServices: RequestHandler = async (req, res, next) => {
  try {
  } catch (error) {}
};

export const availableservices: RequestHandler = async (req, res, next) => {
  try {
  } catch (error) {}
};
